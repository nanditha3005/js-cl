function wish(name){
    return name;
    console.log("GM")   //this is unreachable statement because return is already exceduted so tht it comes out from block
}

let ename=wish("Rahul")
console.log(ename)     //prints only Rahul